# `./components/*`

This folder contains GUI components that makes the app and modules design more reusable.

To have the components available to you during development, add this folder and its subfolders to the MATLAB path for the session before launching MATLAB App Designer. For production, the components will be loaded dynamically at runtime and you do not need to add them to the path permanently.