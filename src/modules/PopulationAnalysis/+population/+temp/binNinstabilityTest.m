function [f, stats] = StimBiasBinInstabilityTest(progresionTable, testBinIndex, kvargs)
    %%BINNINSTABILITYTEST Test for stimulus-bias instability at a given bin index in a progression table.
    % 
    % Automatically groups by "Genotype$" into test groups, then calculate the testBinIndex volatility for each "Stimulus Protocol" for each group.

    
end